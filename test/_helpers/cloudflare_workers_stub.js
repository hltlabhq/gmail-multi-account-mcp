// Stub for `cloudflare:workers`. Loaded by vitest via resolve.alias in
// vitest.config.ts. The OAuth provider library imports WorkerEntrypoint
// for an `instanceof` check; an empty class is enough. In the production
// Workers runtime, the real module is provided.

export class WorkerEntrypoint {}
export class DurableObject {}
